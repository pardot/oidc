syntax = "proto3";

// Package internal holds protobuf types used by the server
package deciserialized;

option go_package = "serializedpb";

// RefreshToken is a message that holds refresh token data used by dex.
message RefreshToken {
  string refresh_id = 1;
  string token = 2;
}